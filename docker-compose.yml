services:
  voice-agent:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - .:/app # For development/hot-reload (remove in prod)
    network_mode: host # Recommended for WebRTC/Voice low latency
